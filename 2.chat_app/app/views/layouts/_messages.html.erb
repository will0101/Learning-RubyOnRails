<% flash.each do |message_type, message_content|%>
<div class="ui container <%= message_type%> message">
  <i class="close icon"></i>
  <div class="header">
  <%= message_content %>
  </div>
</div>
<% end %>