<% if logged_in? && obj.user.id == current_user.id || current_user.admin%>

<%= link_to "Edit", edit_article_path(obj)%>
<%= link_to "Delete", article_path(obj), method: :delete, data: {confirm:"Are you sure?"}%>
<% end %>
